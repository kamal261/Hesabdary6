<?xml version="1.0" encoding="utf-8"?>
<!--
  Blocks adb cloud-backup / device-to-device transfer of the app's financial
  data (database + files). The app's own optional Google Drive backup uses the
  Drive API directly and is unaffected by these rules.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
        <exclude domain="sharedpref" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
        <exclude domain="sharedpref" path="." />
    </device-transfer>
</data-extraction-rules>
