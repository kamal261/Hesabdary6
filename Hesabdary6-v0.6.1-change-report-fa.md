# گزارش تغییرات نسخه v0.6.1 برنامه Hesabdary6

**نام پروژه:** SmsFinance / Hesabdary6  
**شاخه توسعه:** `manus-version`  
**نسخه:** `v0.6.1`  
**کد نسخه:** `8`  
**تاریخ ساخت:** `2026-08-20` / `1405-05-29`  
**توسعه‌دهنده و طراح:** سید کمال حقیقی  
**نوع تحویل:** سورس کامل پروژه در فایل ZIP؛ بدون APK و بدون انتشار در GitHub

## ۱. خلاصه اجرایی

در این نسخه دو قابلیت تأییدشده تکمیل شده است. نخست، کارت‌های تراکنش در صفحه اصلی اکنون به‌جای نمایش نام ساده دسته، مسیر کامل دسته را نشان می‌دهند؛ برای نمونه، مسیر `هزینه / خرید خانه / سوپرمارکت / سیگار` به‌صورت یکپارچه نمایش داده می‌شود. دوم، صفحه مستقل «همه یادداشت‌ها» اضافه شده است تا یادداشت‌های پراکنده در تراکنش‌ها قابل مشاهده، جستجو، بازبینی، کپی و ویرایش باشند.

این تغییرات با اصول محصول پروژه، یعنی **KISS، YAGNI، Offline First و عدم تصمیم مالی بدون تأیید کاربر**، سازگار نگه داشته شده‌اند. هیچ دسته‌بندی، اصلاح مبلغ، حذف تراکنش یا ارتباط مالی به‌صورت خودکار از صفحه یادداشت‌ها انجام نمی‌شود؛ کاربر تنها یادداشت را بازبینی و در صورت نیاز ویرایش می‌کند.

## ۲. تغییرات پیاده‌سازی‌شده

| ردیف | قابلیت | وضعیت | محل اصلی تغییر |
|---:|---|---|---|
| ۱ | نمایش مسیر کامل دسته در کارت‌های صفحه اصلی | تکمیل شد | `ui/screens/TransactionListScreen.kt` |
| ۲ | برچسب‌گذاری روشن بخش دسته با عنوان «دسته فعلی» | تکمیل شد | `ui/screens/TransactionListScreen.kt` |
| ۳ | صفحه مستقل «همه یادداشت‌ها» | تکمیل شد | `ui/screens/NotesScreen.kt` |
| ۴ | جستجو در متن یادداشت، شرح، بانک و مسیر دسته | تکمیل شد | `ui/screens/NotesScreen.kt` |
| ۵ | مشاهده و ویرایش یادداشت در دیالوگ جزئیات | تکمیل شد | `ui/screens/NotesScreen.kt` |
| ۶ | کپی متن یادداشت در کلیپ‌بورد | تکمیل شد | `ui/screens/NotesScreen.kt` |
| ۷ | دسترسی صفحه یادداشت‌ها از صندوق بررسی صفحه اصلی | تکمیل شد | `ui/components/AttentionBanners.kt` و `MainActivity.kt` |
| ۸ | اضافه‌شدن Overlay مستقل برای صفحه یادداشت‌ها | تکمیل شد | `MainActivity.kt` |
| ۹ | به‌روزرسانی راهنمای داخل برنامه | تکمیل شد | `ui/screens/HelpScreen.kt` |
| ۱۰ | ثبت نسخه و کد نسخه جدید | تکمیل شد | `BuildInfo.kt` و `app/build.gradle.kts` |

## ۳. نمایش مسیر کامل دسته در کارت‌های صفحه اصلی

پیش از این تغییر، کارت تراکنش فقط نام آخرین دسته را نمایش می‌داد. این رفتار در دسته‌های چندسطحی باعث می‌شد کاربر نداند تراکنش در کدام شاخه قرار گرفته است؛ برای مثال، نمایش «سیگار» به‌تنهایی ارتباط آن را با «هزینه» و «خرید خانه» مشخص نمی‌کرد.

اکنون هنگام ساخت کارت، مسیر با helper خالص `CategoryTree.pathOf(transaction.categoryId, categories)` محاسبه می‌شود. این روش به جای بازسازی مسیر در رابط کاربری، از منطق واحد موجود در پروژه استفاده می‌کند و با دسته‌های دارای عمق دلخواه سازگار است. در صورت نبود دسته نیز عبارت روشن «بدون دسته» نمایش داده می‌شود.

مسیر در بخش جداگانه‌ای با عنوان **«دسته فعلی»** قرار گرفته است. متن مسیر با `softWrap` در چند خط می‌شکند تا اطلاعات از بین نرود و کارت به‌دلیل مسیرهای طولانی بیش از اندازه فشرده نشود. در نتیجه، کاربر می‌تواند هم دسته فعلی را سریع تشخیص دهد و هم ساختار کامل آن را ببیند.

## ۴. صفحه مستقل «همه یادداشت‌ها»

فایل جدید `NotesScreen.kt` یک صفحه مستقل Compose است که از همان فهرست تراکنش‌های موجود برنامه استفاده می‌کند. فقط تراکنش‌هایی نمایش داده می‌شوند که فیلد `notes` آن‌ها خالی نباشد. یادداشت‌ها بر اساس تاریخ تراکنش، از جدیدترین مورد به قدیمی‌ترین مورد مرتب می‌شوند.

در بالای صفحه، کاربر می‌تواند در متن یادداشت جستجو کند. برای کاربردپذیری بیشتر، جستجو علاوه بر متن یادداشت در شرح تراکنش، نام بانک و مسیر کامل دسته نیز انجام می‌شود. این تصمیم باعث می‌شود کاربر بتواند یادداشت مربوط به یک موضوع را حتی اگر عبارت دقیق یادداشت را به خاطر نداشته باشد، پیدا کند.

| بخش صفحه | رفتار |
|---|---|
| نوار عنوان | نمایش «همه یادداشت‌ها» و دکمه بازگشت |
| کادر جستجو | جستجو در یادداشت، شرح، بانک و مسیر دسته |
| شمارنده | نمایش تعداد یادداشت‌های منطبق با جستجو |
| کارت یادداشت | نمایش بانک، مبلغ، متن یادداشت، مسیر دسته، شرح و تاریخ |
| دیالوگ جزئیات | نمایش متن کامل، امکان ویرایش و کپی یادداشت |
| حالت خالی | پیام جداگانه برای نبود یادداشت یا بی‌نتیجه‌بودن جستجو |

با لمس هر کارت، دیالوگ جزئیات باز می‌شود. در این دیالوگ متن یادداشت در یک فیلد قابل ویرایش قرار می‌گیرد. دکمه کپی فقط متن یادداشت را در کلیپ‌بورد قرار می‌دهد و هیچ داده مالی دیگری را تغییر نمی‌دهد. با ذخیره، مقدار یادداشت از مسیر فعلی `TransactionViewModel.updateTransactionNotes` به Repository منتقل می‌شود و پس از ذخیره، دیالوگ بسته می‌شود.

## ۵. مسیر دسترسی از صفحه اصلی

در `ReviewSummaryCard` تعداد تراکنش‌های یادداشت‌دار محاسبه و نمایش داده می‌شود. اگر حداقل یک یادداشت وجود داشته باشد، دکمه‌ای با عنوان «همه یادداشت‌ها» همراه با تعداد آن‌ها در صندوق بررسی ظاهر می‌شود. لمس این دکمه Overlay مربوط به صفحه یادداشت‌ها را فعال می‌کند و کاربر بدون رفتن به تنظیمات یا جستجوی طولانی می‌تواند به یادداشت‌ها برسد.

برای جلوگیری از ایجاد مسیرهای متعدد و گیج‌کننده، قابلیت جدید از همان صندوق بررسی صفحه اصلی استفاده می‌کند. بنابراین کاربر ابتدا یک نشانه روشن درباره وجود یادداشت می‌بیند و سپس می‌تواند فهرست کامل آن‌ها را باز کند.

## ۶. همسان‌سازی نسخه

مشخصات نسخه در دو محل به‌روزرسانی شد تا مقدار نمایش‌داده‌شده داخل برنامه و مقدار Gradle یکسان باشد.

| مشخصه | مقدار نهایی |
|---|---|
| `versionName` | `0.6.1` |
| `versionCode` | `8` |
| `BUILD_DATE` | `2026-08-20` |
| `BUILD_DATE_SHAMSI` | `1405-05-29` |
| شاخه | `manus-version` |
| توسعه‌دهنده | سید کمال حقیقی |
| طراح | سید کمال حقیقی |

## ۷. بررسی فنی و تست

تست نهایی با دستور زیر اجرا شد:

```bash
export JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64
export ANDROID_HOME=/home/ubuntu/android-sdk
./gradlew :app:testDebugUnitTest --no-daemon
```

نتیجه اجرای نهایی به شرح زیر است:

| شاخص | نتیجه |
|---|---:|
| تست‌های اجراشده | ۷۹ |
| شکست تست | ۰ |
| خطای تست | ۰ |
| نتیجه Gradle | `BUILD SUCCESSFUL` |
| APK ساخته‌شده برای تحویل | خیر |

در زمان کامپایل فقط چند هشدار deprecation مربوط به آیکن‌های قدیمی Material مشاهده شد و هیچ خطای Kotlin یا خطای تست وجود نداشت. این هشدارها مانع ساخت و اجرای تست‌ها نیستند و در محدوده این قابلیت‌ها تغییر داده نشدند.

## ۸. دامنه تغییرات و موارد عمداً انجام‌نشده

این نسخه فقط دو قابلیت درخواست‌شده را تکمیل می‌کند و وارد محدوده‌های پرریسک یا خارج از نیاز فعلی نمی‌شود. هیچ حسابداری دوبل، ترازنامه، ERP، تصمیم مالی خودکار، ادغام خودکار تراکنش‌ها، تغییر ساختار بانک اطلاعاتی یا تغییر در منطق تشخیص پیامک برای این قابلیت‌ها اضافه نشده است.

همچنین برای تحویل نهایی، فایل APK تولید یا ضمیمه نشده است. پروژه در GitHub منتشر یا push نشده و تحویل فقط به‌صورت فایل ZIP سورس کامل انجام می‌شود.

## ۹. فایل‌های اصلی نسخه جدید

فایل‌های زیر در نسخه نهایی نقش مستقیم دارند:

```text
app/src/main/java/com/kamal/smsfinance/ui/screens/TransactionListScreen.kt
app/src/main/java/com/kamal/smsfinance/ui/screens/NotesScreen.kt
app/src/main/java/com/kamal/smsfinance/ui/components/AttentionBanners.kt
app/src/main/java/com/kamal/smsfinance/MainActivity.kt
app/src/main/java/com/kamal/smsfinance/ui/screens/HelpScreen.kt
app/src/main/java/com/kamal/smsfinance/data/CategoryTree.kt
app/src/main/java/com/kamal/smsfinance/BuildInfo.kt
app/build.gradle.kts
```

## ۱۰. نتیجه نهایی

نسخه `v0.6.1` اکنون مسیر کامل دسته را در کارت‌های صفحه اصلی به شکل قابل فهم نمایش می‌دهد و یک محل واحد برای بازبینی همه یادداشت‌های تراکنش‌ها فراهم می‌کند. این تغییر، مشکل گم‌شدن یادداشت‌ها و ابهام دسته‌های چندسطحی را بدون اضافه‌کردن پیچیدگی غیرضروری برطرف می‌کند. سورس کامل در فایل ZIP پیوست شده و فایل گزارش حاضر نیز برای مراجعه و نگهداری در اختیار کاربر قرار می‌گیرد.
