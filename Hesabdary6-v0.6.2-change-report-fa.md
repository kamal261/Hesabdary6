# گزارش تغییرات و فیکس‌های نسخه v0.6.2

**نام پروژه:** SmsFinance / Hesabdary6  
**شاخه توسعه:** `manus-version`  
**نسخه:** `v0.6.2`  
**کد نسخه:** `9`  
**تاریخ ساخت:** `2026-08-22` / `1405-05-31`  
**توسعه‌دهنده و طراح:** سید کمال حقیقی  
**نوع تحویل:** سورس کامل در ZIP؛ بدون APK و بدون انتشار در GitHub

## ۱. خلاصه اجرایی

این نسخه برای اصلاح سه مورد گزارش‌شده در بررسی v0.6.1 آماده شد. مهم‌ترین اصلاح مربوط به بازیابی پشتیبان است: از این پس در بازیابی روی دیتابیس غیرخالی، بررسی فایل، پاک‌سازی و واردکردن داده در مسیر اتمیک انجام می‌شود تا اگر فایل خراب باشد یا در میانه عملیات خطایی رخ دهد، اطلاعات قبلی با rollback حفظ شوند.

همچنین امکان ذخیره تراکنش با ترکیب ناسازگار نوع و دسته بسته شد. برای نمونه، یک تراکنش «هزینه» دیگر نمی‌تواند با دسته «درآمد» ذخیره شود. این محدودیت هم در رابط کاربری و هم در Repository اعمال شده است تا فقط به رفتار صفحه وابسته نباشد.

در نهایت، مسیرهای دسته‌ها به‌صورت یک‌باره برای فهرست فعلی دسته‌ها محاسبه و cache می‌شوند. در نتیجه کارت‌های تراکنش و جستجوی دسته‌ها مجبور نیستند برای هر مورد دوباره map دسته‌ها را بسازند.

## ۲. وضعیت موارد گزارش‌شده

| شناسه | وضعیت در v0.6.2 | نتیجه |
|---|---|---|
| BUG-01 | فیکس شد | Restore جایگزین‌کننده اتمیک شد و در صورت شکست، پاک‌سازی rollback می‌شود |
| BUG-02 | فیکس شد | مسیرهای دسته با `remember(categories)` و `CategoryTree.pathsOf` یک‌بار محاسبه می‌شوند |
| BUG-03 | فیکس شد | UI و Repository ترکیب ناسازگار `TransactionType` و `CategoryKind` را رد می‌کنند |
| BUG-04 | فیکس شد | گزارش جدید، جزئیات فیکس‌ها، قابلیت drill-down و وضعیت تست‌ها را ثبت می‌کند |

## ۳. فیکس BUG-01: Restore امن و اتمیک

### مشکل قبلی

در نسخه قبلی، مسیر تأیید بازیابی ابتدا `wipeForRestore` را اجرا می‌کرد و سپس `performRestore` را صدا می‌زد. این دو عملیات در یک transaction مشترک نبودند. بنابراین فایل خراب یا JSON ناقص می‌توانست بعد از پاک‌شدن اطلاعات قبلی، باعث شکست Restore شود.

### تغییر انجام‌شده

در `BackupManager` تابع جدید `restoreReplacingExisting(...)` اضافه شد. فایل پشتیبان ابتدا خوانده و JSON آن parse می‌شود؛ سپس پاک‌سازی و تمام مراحل Restore داخل یک `db.withTransaction` واحد اجرا می‌شوند. helper پاک‌سازی نیز private شده و فقط داخل همان transaction قابل استفاده است.

مسیر `confirmRestoreReplacingExisting()` در `TransactionViewModel` دیگر پاک‌سازی جداگانه انجام نمی‌دهد و مستقیماً از API اتمیک جدید استفاده می‌کند. در صورت شکست نیز پیام روشن‌تری نمایش داده می‌شود: «بازیابی انجام نشد؛ اطلاعات قبلی حفظ شد».

### نتیجه رفتاری

اگر فایل قبل از شروع transaction خراب باشد، هیچ اطلاعاتی پاک نمی‌شود؛ زیرا parse پیش از حذف انجام شده است. اگر بعد از شروع transaction نیز یکی از فیلدها یا درج‌های میانی خطا بدهد، transaction دیتابیس rollback می‌شود و پاک‌سازی قبلی نیز commit نخواهد شد.

### فایل‌های مرتبط

- `app/src/main/java/com/kamal/smsfinance/util/BackupManager.kt`
- `app/src/main/java/com/kamal/smsfinance/ui/TransactionViewModel.kt`

## ۴. فیکس BUG-03: جلوگیری از ناسازگاری نوع و دسته

### مشکل قبلی

`CategoryPicker` یک سمت داخلی هزینه/درآمد داشت و در مسیر ثبت دستی می‌توانست از نوع تراکنش اصلی جدا شود. در نتیجه امکان ثبت `EXPENSE` با دسته `INCOME` یا ثبت `INCOME` با دسته `EXPENSE` وجود داشت.

### تغییر انجام‌شده در رابط کاربری

پارامتر `allowSideToggle` به `CategoryPicker` اضافه شد. در فرم ثبت دستی، این کنترل خاموش است و سمت دسته از نوع اصلی تراکنش پیروی می‌کند. در صفحه جزئیات تراکنش همچنان امکان تغییر نوع باقی مانده است، چون آنجا تغییر نوع یک اقدام آگاهانه و موجود در جریان اصلاح تراکنش است.

### تغییر انجام‌شده در Repository

قانون خالص `TransactionCategoryRules` اضافه شد و نگاشت مجاز را مشخص می‌کند:

| نوع تراکنش | دسته‌های مجاز |
|---|---|
| `INCOME` | `INCOME` و `DEBT_COLLECTION` |
| `EXPENSE` | `EXPENSE` و `DEBT_PAYMENT` |

این guard در ثبت تراکنش دستی، ثبت تسویه غیرمستقیم، تغییر دسته و update تراکنش اعمال شده است. اگر دسته ناموجود یا ناسازگار باشد، عملیات در Repository رد می‌شود و ViewModel پیام خطا نمایش می‌دهد؛ بنابراین حتی callerهای احتمالی خارج از UI نیز نمی‌توانند به‌سادگی این invariant را دور بزنند.

### نکته حسابداری

تابع `counterpartyBalanceSign()` برای بعضی حالت‌ها fallback دارد؛ بنابراین ادعای «تعریف‌نشدن کامل رفتار» دقیق نبود. بااین‌حال ذخیره ترکیب ناسازگار همچنان داده نامعتبر تولید می‌کند و باید رد شود. این نسخه همین اصل را در لایه دامنه enforce می‌کند.

### فایل‌های مرتبط

- `app/src/main/java/com/kamal/smsfinance/data/TransactionCategoryRules.kt`
- `app/src/main/java/com/kamal/smsfinance/data/TransactionRepository.kt`
- `app/src/main/java/com/kamal/smsfinance/ui/TransactionViewModel.kt`
- `app/src/main/java/com/kamal/smsfinance/ui/components/CategoryPicker.kt`
- `app/src/main/java/com/kamal/smsfinance/ui/screens/AddTransactionScreen.kt`

## ۵. فیکس BUG-02: بهینه‌سازی مسیر دسته

### مشکل قبلی

`CategoryTree.pathOf()` در هر فراخوانی `categories.associateBy { it.id }` را از نو می‌ساخت. در صفحه تراکنش‌ها و هنگام اسکرول، این محاسبه می‌توانست بارها تکرار شود.

### تغییر انجام‌شده

تابع `CategoryTree.pathsOf(categories)` اضافه شد. این تابع map دسته‌ها را یک‌بار می‌سازد و مسیر همه دسته‌ها را برای همان فهرست آماده می‌کند. در `TransactionListScreen` و `CategoryPicker`، این نتیجه با `remember(categories)` نگهداری می‌شود.

کارت تراکنش و دیالوگ جزئیات اکنون مسیر را با lookup ساده از `categoryPaths` می‌خوانند. خروجی مسیر قبلی حفظ شده است؛ برای نمونه، مسیر `هزینه / خرید خانه / سوپرمارکت` همچنان همان‌طور نمایش داده می‌شود.

### فایل‌های مرتبط

- `app/src/main/java/com/kamal/smsfinance/data/CategoryTree.kt`
- `app/src/main/java/com/kamal/smsfinance/ui/screens/TransactionListScreen.kt`
- `app/src/main/java/com/kamal/smsfinance/ui/components/CategoryPicker.kt`

## ۶. تست‌های افزوده‌شده

برای قانون نوع و دسته، تست‌های زیر در `TransactionCategoryRulesTest` اضافه شدند: پذیرش دسته‌های مجاز برای درآمد، پذیرش دسته‌های مجاز برای هزینه، رد دسته‌های سمت مخالف و مجازبودن تراکنش بدون دسته.

برای cache مسیر دسته نیز تست `precomputed_paths_match_single_path_lookup` به `CategoryTreeTest` اضافه شد تا خروجی محاسبه یک‌باره با خروجی helper قبلی یکسان بماند.

پوشش مستقیم تستی Restore خراب هنوز نیازمند تست instrumentation با دیتابیس واقعی و فایل پشتیبان خراب است؛ منطق اتمیک در کد اعمال شده، اما این سناریوی دستگاهی در اجرای فعلی به emulator و تست Android نیاز دارد و جزو تست واحد JVM نیست.

## ۷. نتیجه اجرای تست

دستور زیر پس از فیکس‌ها اجرا شد:

```bash
export JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64
export ANDROID_HOME=/home/ubuntu/android-sdk
./gradlew :app:testDebugUnitTest --no-daemon
```

نتیجه واقعی:

| شاخص | نتیجه |
|---|---:|
| تست‌های اجراشده | ۸۴ |
| شکست | ۰ |
| خطا | ۰ |
| نتیجه Gradle | `BUILD SUCCESSFUL` |
| APK تحویلی | خیر |

هشتاد و چهار تست شامل تست‌های قبلی و تست‌های جدید قانون نوع/دسته و مسیرهای cache شده است. چند هشدار deprecation در کد وجود دارد، اما خطای کامپایل یا شکست تست دیده نشد.

## ۸. نسخه و دامنه تحویل

نسخه در هر دو محل زیر به `v0.6.2` و کد `9` تغییر کرد:

- `app/build.gradle.kts`
- `app/src/main/java/com/kamal/smsfinance/BuildInfo.kt`

تاریخ نسخه `2026-08-22` / `1405-05-31` ثبت شد. ZIP نهایی فقط سورس پروژه و گزارش‌ها را شامل می‌کند و نباید شامل `.git`، پوشه‌های `build`، `local.properties` یا APK باشد.

## ۹. مواردی که عمداً تغییر نکردند

این فیکس‌ها حسابداری دوبل، ترازنامه، ERP، تصمیم مالی خودکار یا حذف خودکار تراکنش‌ها را اضافه نمی‌کنند. Restore فقط با تأیید صریح کاربر انجام می‌شود. سیستم در تشخیص ارتباط چک یا انتقال شخصی همچنان پیشنهاد می‌دهد و تصمیم نهایی را به کاربر واگذار می‌کند.

## نتیجه نهایی

گزارش باگ v0.6.1 از نظر سه ایراد اصلی درست بود. نسخه v0.6.2 هر سه ایراد فنی را اصلاح می‌کند: Restore جایگزین‌کننده اکنون اتمیک است، انتخاب ناسازگار نوع و دسته هم در UI و هم در Repository بسته شده، و محاسبه مسیر دسته از حالت تکراری خارج شده است. گزارش حاضر نیز BUG-04 را پوشش می‌دهد و محدودیت باقی‌مانده، یعنی نیاز به تست instrumentation برای Restore خراب، را صریح ثبت می‌کند.

**در این نسخه APK ساخته یا تحویل نشده و هیچ فایلی در GitHub منتشر نشده است.**
