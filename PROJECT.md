# SmsFinance — راهنمای پروژه (Project Memory)

این فایل حافظه‌ی پروژه SmsFinance است. هر جزئیاتی که موقع کار روی این
پروژه لازم است اینجا ذخیره می‌شود تا نیازی به نگه‌داشتن آن در حافظه‌ی
اصلی Hermes نباشد. این فایل همیشه همراه پروژه در ریپو است و با بکاپ هم
پشتیبان‌گیری می‌شود.

---

## معرفی محصول

اپ اندروید «SmsFinance» یک پارسر ساده پیامک بانکی برای کسب‌وکارهای کوچک:

- **۴ دسته:** درآمد / هزینه / وصول بدهی / پرداخت بدهی
- **مدیریت طرف‌حساب‌ها:** مشتری‌ها در برابر کارگرها
- **یادآوری چک** و **یادآوری پرداخت غیرمستقیم**
- **بکاپ گوگل‌درایو**
- **بدون ویژگی‌های حسابداری پیچیده:** بدون دفتر کل، فاکتور، ترازنامه، مالیات
- فقط ورود/خروج پول، چه‌کسی به چه‌کسی بدهکاره، گزارش‌های ساده

## مشخصات فنی

- زبان: Kotlin + Jetpack Compose
- معماری: MVVM + Repository Pattern
- دیتابیس: Room
- مین SDK: 26، هدف: 34
- دسترسی: READ_SMS / RECEIVE_SMS

## بیلد APK

- ریپو: `kamal261/Hesabdary6` (شاخه `main`)
- بیلد از طریق GitHub Actions (workflow: build-apk.yml)
- توکن حساب: `kamal261` (جدا از حساب بکاپ `kamal266-1` — هرگز قاطی نشود)
- بیلد فقط بعد از تأیید صریح کاربر
- اگه توکن در دسترس نبود: STOP و بپرس (هرگز در فایل ذخیره نکن)

### باگ تکرارشونده (مهم!)
فایل‌های SmsFinance.zip که کاربر آپلود می‌کند، ایمپورت
`import androidx.compose.foundation.layout.padding`
را در `MainActivity.kt` از دست می‌دهند → خطای بیلد:
`MainActivity.kt: Unresolved reference: padding`
**رفع:** بعد از هر آپلود، این ایمپورت را اضافه کن.
(جزئیات بیشتر: skill اندروید `android-sms-finance-apk-build`)

## قوانین فیلتر تراکنش (نسخه 0.0.3)

- **تراکنش فقط از سمت بانک** (فرستنده کوتاه‌کد بانک یا نام بانک در متن)
- رد: OTP/رمز پویا، تبلیغ (تسهیلات/اعتبار/قسط/جایزه...)، URL، اپراتور تلفن، فروشگاه، پیامک شخصی
- «از حساب شما پرید» = تراکنش بلو محسوب می‌شود ✅
- تیل کارت شامل `****1234` و `621986*0771` امضای بانکی است
- **تاریخ تراکنش = تاریخ داخل متن پیامک** (نه تاریخ دریافت)
- اسکن اولیه: از کاربر بازه می‌پرسد (۷ روز/۱ ماه/۳ ماه/۱ سال/همه)، هرگز دوباره اسکن نمی‌کند
- تست‌های فیلتر → skill `iranian-bank-sms`

## وضعیت نسخه‌ها

| نسخه | تغییرات اصلی | وضعیت |
|---|---|---|
| 1.0.0 | پایه (از کاربر) | — |
| 0.0.2 | + بیلد اولیه روی Actions | تحویل شد |
| 0.0.3 | + فیلتر هوشمند بانک، SMS context، اسکن بازه، لیست بانک، تاریخ شمسی | تحویل شد (Aug 2026) |

## تاریخچه تصمیم‌های مهم

- **1405/05:** فیلتر sender-based شد (چون «پرداخت صورتحساب» اپراتور هم مبلغ و شناسه دارد ولی تراکنش نیست)
- **1405/05:** تاریخ ملاک، داخل متن پیامک است
- **1405/05:** اسکن اولیه فقط یک‌بار با انتخاب بازه توسط کاربر
