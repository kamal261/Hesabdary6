# گزارش تغییرات v0.6.6

## باگ فیکس‌شده

**ریال/تومان در `BARE_AMOUNT_REGEX`** — پیامک‌هایی مثل «انتقال به مبلغ 2,000,000» (بدون کلمه‌ی ریال/تومان) قبلاً بدون تقسیم بر ۱۰ ذخیره می‌شدن (۱۰ برابر واقعی). ریشه: یکی از سه مسیر استخراج مبلغ در `SmsParser.kt`، برخلاف دو مسیر دیگه، تقسیم رو انجام نمی‌داد. تست رگرسیون هم اضافه شد (`SmsParserTest.kt`).
⚠️ این فیکس فقط جلوی ثبت‌های *بعدی* رو می‌گیره. تراکنش‌های قبلی با همین الگو (بانک ملی، بدون کلمه‌ی واحد) در دیتابیس با مبلغ ۱۰ برابر ذخیره شدن و نیاز به بررسی/اصلاح دستی دارن.

## قابلیت‌های جدید

1. **قوانین «نادیده‌گرفتن»** — `SmartRule` حالا یک `action` داره (`CATEGORIZE` یا `IGNORE`). پیامک‌های منطبق با یک قانون IGNORE، نه تراکنش می‌شن نه در صف «نیاز به بررسی» ظاهر می‌شن. دو نقطه‌ی ورودی: صفحه‌ی «قوانین هوشمند» (ساخت دستی) و صفحه‌ی «پیامک‌های نیازمند بررسی» (دکمه‌ی «همیشه نادیده بگیر» روی هر مورد).
2. **جابجایی دستی بین حساب‌های خودم** — علاوه بر تشخیص خودکار قبلی (`TransferDetector`، مبلغ دقیقاً برابر + فاصله زیر ۱۵ دقیقه)، حالا از دیالوگ جزئیات هر تراکنش می‌شه دستی دو تراکنش رو به‌هم وصل کرد (بدون محدودیت مبلغ/زمان). امکان لغو پیوند هم اضافه شد (قبلاً نبود).
3. **راهنمای دسته‌ی والد** — توضیح ساده و با مثال، هنگام ساخت دسته‌ی جدید.

## فیکس‌های زیرساختی

4. **تکرار پیامک‌های نیازمند بررسی بعد از قطع/راه‌اندازی مجدد** — علت: `existsExact()` در DAO تعریف شده بود ولی هیچ‌جا صدا زده نمی‌شد؛ تنها محافظ یک `ConcurrentHashMap` در حافظه بود که با هر بار بسته‌شدن اپ ریست می‌شد. حالا چک دیتابیسی واقعی وصل شده + ایندکس جدید برای کارایی.
5. حذف بنر پیشنهاد بک‌آپ از صفحه‌ی اول (فقط در تنظیمات باقی موند).

## تغییرات schema

نسخه‌ی دیتابیس از ۹ به ۱۱ رفت (دو migration جدید: ایندکس `unidentified_sms`، ستون `action` روی `smart_rules`).

## ⚠️ قبل از build

- فایل‌های `app/schemas/.../10.json` و `11.json` باید با اجرای واقعی Gradle تولید بشن.
- توصیه می‌شه `./gradlew testDebugUnitTest` واقعی اجرا بشه؛ تأیید این نسخه فقط با بازخوانی دستی کد انجام شده، نه اجرای کامپایلر.
- بند ۸ (دسته‌های پیش‌فرض نقش‌محور) و بند ۴ فهرست اصلی (که در واقع از قبل وجود داشت) خارج از دامنه‌ی این نسخه بودن.
