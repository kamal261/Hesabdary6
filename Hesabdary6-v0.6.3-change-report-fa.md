# گزارش تغییرات نسخه v0.6.3

**نام پروژه:** SmsFinance / Hesabdary6  
**شاخه توسعه:** `manus-version`  
**نسخه:** `v0.6.3`  
**کد نسخه:** `10`  
**تاریخ ساخت:** `2026-08-22` / `1405-05-31`  
**توسعه‌دهنده و طراح:** سید کمال حقیقی  
**نوع تحویل:** سورس کامل در ZIP؛ بدون APK و بدون انتشار در GitHub

## خلاصه

در این نسخه مسیر انتخاب دسته برای تراکنش‌های بانکی ساده‌تر شد. وقتی پیامک نشان‌دهنده برداشت از حساب باشد، برنامه فقط دسته‌های هزینه را نمایش می‌دهد. وقتی پیامک نشان‌دهنده واریز باشد، برنامه فقط دسته‌های درآمد را نمایش می‌دهد. کاربر دیگر در مسیر عادی مجبور نیست بین هزینه و درآمد انتخاب کند.

برای شرایط استثنایی، مانند تشخیص اشتباه نوع تراکنش، گزینه روشن و جداگانه «تغییر نوع» در صفحه جزئیات تراکنش باقی مانده است. با انتخاب این گزینه، برنامه از کاربر تأیید می‌گیرد و بعد از تغییر نوع، دسته قبلی را پاک می‌کند تا دسته درست دوباره انتخاب شود.

این طراحی برای کاربران غیرمتخصص انجام شد تا صفحه اصلی خلوت‌تر، قابل‌فهم‌تر و کم‌خطاتر باشد؛ در عین حال انعطاف لازم برای اصلاح اشتباه‌های واقعی حفظ شده است.

## رفتار جدید برنامه

| وضعیت تراکنش | رفتار اصلی |
|---|---|
| برداشت از حساب | نمایش فقط دسته‌های هزینه و زیرشاخه‌های هزینه |
| واریز به حساب | نمایش فقط دسته‌های درآمد و زیرشاخه‌های درآمد |
| ثبت دستی تراکنش | کاربر ابتدا نوع را در فرم ثبت دستی انتخاب می‌کند و سپس دسته‌های همان نوع را می‌بیند |
| تشخیص اشتباه نوع | استفاده از گزینه «تغییر نوع» در صفحه جزئیات تراکنش |
| انتقال بین حساب‌های خود کاربر | همچنان از مسیر پیشنهاد انتقال شخصی بررسی و با تأیید کاربر ثبت می‌شود |
| برداشت نقدی از خودپرداز | همچنان می‌تواند بدون دسته بماند تا کاربر بعداً کاربرد پول را مشخص کند |

## تغییرات فنی

### ۱. قفل‌شدن سمت دسته در ثبت دستی

در `CategoryPicker` پارامتر `allowSideToggle` اضافه شد. فرم ثبت دستی این مقدار را `false` می‌فرستد؛ بنابراین پس از انتخاب «هزینه» یا «درآمد» در فرم، کنترل دسته‌بندی دیگر سمت مخالف را نشان نمی‌دهد.

### ۲. قفل‌شدن سمت دسته در جزئیات تراکنش

در `TransactionDetailDialog` نیز CategoryPicker با `allowSideToggle = false` اجرا می‌شود. نوع تراکنش از همان مقدار واقعی تراکنش پیروی می‌کند؛ بنابراین برداشت، دسته‌های درآمد را نشان نمی‌دهد و واریز، دسته‌های هزینه را نشان نمی‌دهد.

### ۳. باقی‌ماندن مسیر استثنا

برای اینکه برنامه در صورت تشخیص نادرست SMS انعطاف خود را از دست ندهد، دکمه «تغییر نوع» به‌صورت جداگانه در صفحه جزئیات باقی ماند. پنجره توضیح می‌دهد که این تغییر باید فقط در صورت اطمینان انجام شود. ViewModel پس از تغییر نوع، دسته قبلی را پاک می‌کند و کاربر می‌تواند دسته سازگار جدید را انتخاب کند.

### ۴. حفظ گارد Repository

گارد نسخه قبل، یعنی `TransactionCategoryRules`، همچنان در Repository فعال است. بنابراین حتی اگر caller دیگری خارج از UI بخواهد دسته ناسازگار را ذخیره کند، عملیات رد می‌شود. این دفاع دومرحله‌ای هم تجربه کاربر را ساده می‌کند و هم تمامیت داده را حفظ می‌کند.

### ۵. به‌روزرسانی راهنمای داخل برنامه

بخش «تراکنش‌ها» در `HelpScreen.kt` به‌روز شد تا رفتار جدید برداشت/واریز و مسیر «تغییر نوع» را با نام واقعی گزینه‌ها توضیح دهد.

## فایل‌های تغییرکرده

- `app/src/main/java/com/kamal/smsfinance/ui/components/CategoryPicker.kt`
- `app/src/main/java/com/kamal/smsfinance/ui/screens/AddTransactionScreen.kt`
- `app/src/main/java/com/kamal/smsfinance/ui/screens/TransactionListScreen.kt`
- `app/src/main/java/com/kamal/smsfinance/ui/screens/HelpScreen.kt`
- `app/src/main/java/com/kamal/smsfinance/BuildInfo.kt`
- `app/build.gradle.kts`

فیکس‌های پایه نسخه قبل نیز در سورس باقی هستند؛ از جمله Restore اتمیک، validation در Repository، cache مسیر دسته، صفحه همه یادداشت‌ها و مسیر کامل دسته در کارت تراکنش.

## نتیجه تست

تست واحد نسخه v0.6.3 پس از تغییرات با دستور زیر اجرا شد:

```bash
export JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64
export ANDROID_HOME=/home/ubuntu/android-sdk
./gradlew :app:testDebugUnitTest --no-daemon
```

| شاخص | نتیجه |
|---|---:|
| تست‌های اجراشده | ۸۴ |
| شکست | ۰ |
| خطا | ۰ |
| نتیجه Gradle | `BUILD SUCCESSFUL` |
| APK تحویلی | خیر |

تست‌های قبلی مربوط به قانون سازگاری نوع و دسته و مسیرهای cache‌شده نیز همچنان موفق هستند. چند هشدار deprecation مربوط به APIهای قدیمی Android/Material وجود دارد، اما هیچ خطای کامپایل یا شکست تست مشاهده نشد.

## نسخه و بسته تحویل

`versionName` به `0.6.3` و `versionCode` به `10` تغییر کرد. مشخصات داخل `BuildInfo.kt` و تنظیمات Gradle با یکدیگر همسان هستند. تاریخ نسخه `2026-08-22` / `1405-05-31` ثبت شد.

ZIP تحویلی فقط سورس کامل و گزارش‌ها را شامل می‌کند. فایل‌های `.git`، `.gradle`، پوشه‌های `build`، `local.properties`، APK، AAB و `.hermes` از آن حذف شده‌اند. هیچ تغییری در GitHub انجام نشده است.

## تصمیم طراحی و محدودیت آن

برداشت بانکی در بیشتر حالت‌ها خرج‌کردن پول است، اما همیشه هزینه واقعی نیست؛ ممکن است انتقال بین حساب‌های خود کاربر یا برداشت نقدی باشد. به همین دلیل برنامه در مسیر اصلی فقط هزینه را نشان می‌دهد، ولی گزینه «تغییر نوع» و جریان‌های مستقل انتقال شخصی و برداشت بدون دسته را حذف نمی‌کند.

این انتخاب با اصول KISS، YAGNI، Offline First و «هیچ تصمیم مالی بدون تأیید کاربر» سازگار است: مسیر معمول کوتاه و ساده است، اما استثناهای مهم همچنان قابل بررسی و تأیید دستی هستند.

## نتیجه نهایی

نسخه v0.6.3 انتخاب هزینه/درآمد را از مسیر عادی تراکنش‌های بانکی حذف و تصمیم اولیه را از روی برداشت یا واریز مشخص می‌کند. این تغییر برای کاربر کم‌سواد، تعداد انتخاب‌های گیج‌کننده را کم می‌کند و هم‌زمان با نگه‌داشتن «تغییر نوع» در جزئیات، امکان اصلاح موارد استثنایی را حفظ می‌کند.
