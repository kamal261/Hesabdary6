# CHANGELOG — SmsFinance

این فایل تاریخچهٔ کامل تغییرات فنی پروژه رو نگه می‌داره. از این به بعد، هر فایلی
که دست بخوره، بالای همون فایل یه کامنت `// SmsFinance file version: N` اضافه/آپدیت
می‌شه، و همون تغییر اینجا هم ثبت می‌شه — تا بتونی نسخه‌های مختلف یه فایل رو از هم
تشخیص بدی.

---

## وضعیت فعلی نسخه‌های فایل (فقط فایل‌هایی که دست خوردن)

| فایل | نسخه فعلی |
|---|---|
| `sms/SmsParser.kt` | v2 |
| `data/UnidentifiedSms.kt` | v1 |
| `data/UnidentifiedSmsDao.kt` | v1 |
| `data/Counterparty.kt` | v1 |
| `data/AppDatabase.kt` | v2 |
| `data/TransactionRepository.kt` | v2 |
| `util/SettingsStore.kt` | v1 |
| `util/CsvImporter.kt` | v1 |
| `util/BackupManager.kt` | v2 |
| `ui/theme/Color.kt` | v2 |
| `ui/theme/Theme.kt` | v1 |
| `ui/components/CategoryPicker.kt` | v2 |
| `ui/components/TodayDashboardCard.kt` | v1 |
| `ui/screens/UnidentifiedSmsScreen.kt` | v1 |
| `ui/TransactionViewModel.kt` | v2 |
| `ui/screens/TransactionListScreen.kt` | v3 |
| `ui/screens/AddTransactionScreen.kt` | v2 |
| `ui/screens/CounterpartyProfileScreen.kt` | v1 |
| `ui/screens/SettingsScreen.kt` | v2 |
| `MainActivity.kt` | v2 |
| `SmsFinanceApp.kt` | v1 |

*(بقیه فایل‌های پروژه هنوز وارد این سیستم شماره‌گذاری نشدن چون دست نخوردن.)*

---

## تاریخچهٔ تجمیعی (پیش از شروع شماره‌گذاری فایل به فایل)

### مرحله ۱ — ساخت اولیهٔ اپ
پروژهٔ کامل اندروید (Kotlin + Compose + Room) با: تشخیص پیامک بانکی (۱۵+ بانک)،
ذخیره محلی، خروجی CSV، پشتیبان‌گیری محلی، UI کامل (لیست/افزودن دستی/آمار/تنظیمات)،
دارک‌مود.

### مرحله ۲ — گسترش قابلیت‌ها
افزودن: دسته‌بندی تراکنش (Category)، طرف‌حساب مشتری/عامل (Counterparty)،
مدیریت چک (Check)، یادآوری تسویه غیرمستقیم، پشتیبان‌گیری Google Drive،
اتصال اختیاری Google Sheets.

### مرحله ۳ — اصلاح معماری (Single Source of Truth)
حذف فیلد ذخیره‌شدهٔ `isRecurring` از `Transaction` و جایگزینی با محاسبهٔ آنی
(`RecurringDetector.computeRecurringIds`) — چون داده مشتق‌شده نباید ذخیره بشه.

### مرحله ۴ — موتور قوانین هوشمند (Rule Engine)
افزودن `SmartRule` + `RuleEngine` + صفحهٔ «قوانین هوشمند» — دسته‌بندی خودکار
تراکنش‌های تکراری بر اساس الگوی متنی قابل مشاهده/ویرایش/حذف توسط کاربر.

### مرحله ۵ — رفع مشکلات یافته‌شده در دو ریویوی فنی/حسابداری
- باگ بحرانی: `smart_rules` از بکاپ/بازیابی جا افتاده بود → اضافه شد.
- `RecurringDetector`: تولرانس ۱۰٪ روی مبلغ (برای اجاره/حقوق متغیر).
- Dedup: لایهٔ پشتیبان با `accountTail` برای وقتی بانک sender code عوض می‌کنه.
- خروجی CSV: ستون «منبع» حالا «تسویه چک»/«تسویه غیرمستقیم» رو هم جدا نشون می‌ده.
- آمار: کارت مستقل «سود تقریبی این ماه» (بدون احتساب وصول طلب/پرداخت بدهی) +
  کپشن هشدار که این سود حسابداری/مالیاتی نیست.
- پیشنهاد نرم (غیرمسدودکننده) انتخاب طرف‌حساب برای دسته‌های بدهی/طلب.
- افزودن `id` تراکنش به payload ارسالی به Google Sheets.
- بازآرایی README (فقط نصب/ساختار فنی) در برابر Report (فقط معرفی محصول).
- نرم‌کردن ادعای «۱۵+ بانک» به عبارت دقیق‌تر.

### مرحله ۶ — راه‌اندازی مسیر ساخت APK کاملاً آنلاین
افزودن `.github/workflows/build-apk.yml` (بیلد خودکار در GitHub Actions)،
تولید آیکون پیش‌فرض launcher، افزودن فایل‌های Gradle Wrapper (`gradlew`,
`gradle-wrapper.jar`, ...) که از اول پروژه وجود نداشتن. بعداً یه مرحلهٔ
Extract خودکار zip هم به workflow اضافه شد چون آپلود پوشه‌ای از موبایل ناقص بود.

### مرحله ۷ — رفع باگ تبدیل ریال/تومان (گزارش‌شده با پیامک واقعی)
`SmsParser.kt`: وقتی پیامک هیچ واحد پولی نداشت (مثل بانک رسالت)، عدد اشتباهی
به‌عنوان تومان (به‌جای ریال) ثبت می‌شد. اضافه شد:
- پیش‌فرض ریال برای مبالغ بدون واحد.
- تشخیص نوع تراکنش از روی علامت `+`/`-` وقتی کلیدواژه‌ای نیست.
- افزودن بانک‌های «رسالت» و «بلو» به لیست شناسایی.

### مرحله ۸ — نمایش کامل تراکنش
`TransactionListScreen.kt`: تپ روی هر تراکنش حالا یه دیالوگ کامل باز می‌کنه که
متن کامل پیامک بانکی + مبلغ + بانک + تاریخ دقیق رو نشون می‌ده، و از همون‌جا
دسته‌بندی هم قابل انتخابه — برای وقتی که چندین پیامک مشابه رو داری بررسی می‌کنی.

### مرحله ۹ — رفع رگرسیون false-positive (پیامک تبلیغاتی به‌عنوان تراکنش)
`SmsParser.kt` (v1 طبق سیستم شماره‌گذاری جدید): فیلتر تشخیص علامت `+`/`-`
(که در مرحلهٔ ۷ اضافه شده بود) بیش‌ازحد شل بود و پیامک‌های تبلیغاتی/شماره‌تلفن/
کد تخفیف رو هم به اشتباه تراکنش تشخیص می‌داد. اضافه شد: این تشخیص فقط وقتی
فعال می‌شه که پیامک یه خط «مانده:»/«موجودی:» هم داشته باشه (امضای مختص
پیامک بانکی واقعی).

---

### مرحله ۱۰ — پیاده‌سازی کامل سند «کپی.txt» + بازطراحی ظاهری

**آیتم ۶ (جایگزین تاریخچهٔ خودکار پیامک):**
- `SmsParser.parse()` حالا یک نتیجهٔ sealed برمی‌گردونه (`Recognized` / `Unidentified` / `Ignored`) به‌جای مقدار nullable ساده.
- پیامک‌هایی که شبیه بانکی هستن (کلیدواژه داشتن، یا فرستنده‌شون یه بانک شناخته‌شده‌ست) ولی کامل پارس نشدن، به‌جای حذف بی‌صدا، تو جدول جدید `unidentified_sms` ذخیره می‌شن.
- صفحهٔ «پیامک‌های شناسایی‌نشده» (از تنظیمات یا بنر بالای لیست تراکنش‌ها) برای مرور و «نادیده گرفتن» دستی.

**آیتم ۱ — تجربهٔ انتخاب دسته:** کامپوننت مشترک `CategoryPicker` (۴ دستهٔ پرکاربرد محاسبه‌شده به‌صورت زنده از تعداد تراکنش‌ها + دکمهٔ «بیشتر» با جستجو و فیلتر نوع). در `AddTransactionScreen` و دیالوگ جزئیات تراکنش هر دو استفاده می‌شه.

**آیتم ۲ — Import CSV:** `CsvImporter` برای دسته‌ها (`name,kind`) و طرف‌حساب‌ها (`name,type,...`)، یک‌طرفه، با نادیده‌گرفتن موارد تکراری بر اساس نام (بدون حساسیت به حروف)، دسته‌های پیش‌فرض دست‌نخورده می‌مونن. دکمه‌ها در تنظیمات.

**آیتم ۳ — یادداشت طرف‌حساب:** فیلد `notes` به `Counterparty` اضافه شد؛ قابل ویرایش از صفحهٔ پروفایل طرف‌حساب.

**آیتم ۴ — مشاهدهٔ کامل تراکنش:** دیالوگ جزئیات تراکنش حالا `smsSender` و چهار رقم آخر حساب (`accountTail`) رو هم نشون می‌ده (متن کامل پیامک از قبل اضافه شده بود).

**آیتم ۵ — دسته‌بندی خودکار مبالغ کوچک:** تنظیم آستانه + انتخاب دستهٔ مقصد (از دسته‌های موجود کاربر، نه یه «متفرقه» ثابت) + کلید فعال/غیرفعال؛ فقط روی هزینه‌های SMS اعمال می‌شه و اگه یه قانون هوشمند قبلاً دسته‌بندی کرده باشه، اولویت با قانونه.

**آیتم ۷ — بهبودهای قبلی:**
- فیلتر «فقط بدون دسته» در لیست تراکنش‌ها.
- کارت داشبورد «وضعیت امروز» بالای لیست: ورودی/خروجی امروز، سود تقریبی این ماه، طلب/بدهی کل، تعداد چک نزدیک سررسید — همه بدون اسکرول.
- ثبت دستی بازطراحی شد: مسیر سریع (نوع → مبلغ → دکمهٔ بزرگ تأیید) پیش‌فرضه؛ بانک/توضیحات/دسته/طرف‌حساب پشت «جزئیات بیشتر» جمع شدن.
- رفع باگ mapping آیدی در Restore: دسته‌ها/طرف‌حساب‌ها حالا بر اساس نام dedupe می‌شن (نه insert کورکورانه)؛ اگه دیتابیس خالی نباشه، قبل از بازیابی یه دیالوگ تأیید صریح نشون داده می‌شه.
- رنگ هزینه از قرمز خالص به یه coral/نارنجی ملایم‌تر تغییر کرد.

**آیتم ۸:** بدون تغییر کد — فقط تأیید مجدد مرزبندی (بدون موجودی/انبار/کارگاه).

**بازطراحی ظاهری (درخواست جداگانه):**
- پالت رنگی تازه: هزینه به coral نرم، درآمد به سبز زمردی غنی‌تر، رنگ‌های اختصاصی برای هر kind دسته، کانتینر بنفش ملایم برای کارت داشبورد.
- غیرفعال‌کردن `dynamicColor` پیش‌فرض (قبلاً روی اندروید ۱۲+ پالت اپ رو نادیده می‌گرفت و رنگ‌های wallpaper رو نشون می‌داد).
- کارت داشبورد «امروز» با گوشه‌های گردتر و رنگ اختصاصی به‌عنوان المان اصلی بصری صفحهٔ اول.
- دکمه‌های بزرگ‌تر و رنگی برای انتخاب نوع تراکنش در فرم ثبت دستی.

---

## موارد بررسی‌شده ولی هنوز پیاده‌سازی‌نشده

سند `کپی.txt` (۸ آیتم) به‌طور کامل پیاده‌سازی شد (مرحلهٔ ۱۰ بالا). موردی از آن سند باز نمانده است.

