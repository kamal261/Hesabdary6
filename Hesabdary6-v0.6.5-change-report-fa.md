# گزارش تغییرات نسخه v0.6.5

**نام پروژه:** SmsFinance / Hesabdary6  
**شاخه توسعه:** `manus-version`  
**نسخه:** `v0.6.5`  
**کد نسخه:** `12`  
**تاریخ:** `2026-08-23` / `1405-06-01`  
**توسعه‌دهنده و طراح:** سید کمال حقیقی  
**نوع تحویل:** ZIP سورس کامل؛ بدون APK و بدون انتشار در GitHub

## ۱. خلاصه

این نسخه پیشنهادهای فوری UI/UX و اعتماد کاربر را که در گزارش مرجع اولویت‌بندی شده بودند، روی سورس اعمال می‌کند. هدف اصلی، ساده‌تر شدن برنامه برای کاربر فارسی‌زبان غیرمتخصص، خوانایی بهتر، کاهش لمس اشتباه و کوتاه شدن فاصله بین اولین اجرای برنامه و دیدن نتیجه اسکن است.

اصلاحات مربوط به منطق حسابداری، Restore اتمیک، اعتبارسنجی نوع و دسته و cache مسیر دسته از نسخه‌های قبل حفظ شده‌اند و در این نسخه حذف یا تضعیف نشده‌اند.

## ۲. تغییرات اجراشده

### ۲.۱. راست‌به‌چپ سراسری

در `ui/theme/Theme.kt`، کل محتوای `SmsFinanceTheme` داخل `CompositionLocalProvider` با `LayoutDirection.Rtl` قرار گرفت. در نتیجه چیدمان اصلی برنامه به زبان سیستم گوشی وابسته نیست و روی گوشی‌ای که زبان سیستم آن انگلیسی است نیز فارسی و راست‌به‌چپ باقی می‌ماند.

مقادیر محلی مرتبط با متن‌های ترکیبی، SMS، اعداد و شماره حساب باید در تست دستگاه واقعی بررسی شوند؛ RTL سراسری جهت چیدمان را کنترل می‌کند و به معنی برعکس‌کردن دستی محتوای عددی نیست.

### ۲.۲. فونت فارسی و خوانایی

فونت آزاد Vazirmatn با وزن‌های Regular، Medium، SemiBold و Bold به `app/src/main/res/font` افزوده شد. `ui/theme/Type.kt` اکنون Typography برنامه را با همین خانواده فونت می‌سازد.

اندازه‌های اصلی نیز برای خوانایی بیشتر افزایش یافته‌اند: متن بدنه، عنوان‌ها، برچسب‌ها و تیترها اندازه و فاصله خط مناسب‌تری دارند. مجوز فونت نیز در فایل زیر ثبت شده است:

```text
licenses/Vazirmatn-copyright.txt
```

### ۲.۳. افزایش کنتراست رنگ‌های مالی

رنگ هزینه و درآمد از طیف‌های عمیق‌تر انتخاب شد تا نوشته مبلغ روی زمینه روشن خواناتر باشد، بدون آنکه هزینه با حالت خطا اشتباه گرفته شود. هزینه همچنان با طیف خاکی/مرجانی و درآمد با سبز مشخص می‌شود.

برای جلوگیری از وابستگی به رنگ، کارت تراکنش از آیکون جهت و متن «برداشت/واریز» نیز استفاده می‌کند.

### ۲.۴. کارت تراکنش خلوت‌تر و امن‌تر

در `TransactionListScreen.kt` تغییرات زیر اعمال شد:

- مبلغ در سمت راست با فونت بزرگ‌تر و برجسته‌تر نمایش داده می‌شود؛
- برچسب کوچک «برداشت» یا «واریز» کنار مبلغ قرار گرفته است؛
- سه چیپ بزرگ وضعیت به علامت‌های فشرده‌تر تبدیل شدند تا کارت ارتفاع و شلوغی کمتری داشته باشد؛
- مسیر دسته حداکثر در دو خط نمایش داده می‌شود و در مسیر خیلی طولانی با سه‌نقطه کوتاه می‌شود؛
- آیکون حذف مستقیم از کارت برداشته شد؛
- حذف فقط از صفحه جزئیات انجام می‌شود و پیش از حذف، دیالوگ تأیید نمایش داده می‌شود.

این تغییرات، حذف ناخواسته را سخت‌تر و پیدا کردن مبلغ را سریع‌تر می‌کند.

### ۲.۵. دکمه واقعی در صفحه خالی

`EmptyState` اکنون در زمانی که هنوز هیچ تراکنشی وجود ندارد، دو دکمه واضح دارد:

- `اسکن پیامک‌ها`؛
- `ثبت دستی تراکنش`.

اگر صفحه فقط به‌دلیل جستجو یا فیلتر خالی باشد، دکمه‌های شروع نشان داده نمی‌شوند و پیام «موردی با این جستجو پیدا نشد» نمایش داده می‌شود.

### ۲.۶. توضیح مجوز پیامک و متن حریم خصوصی

صفحه توضیح پیش از پنجره رسمی مجوز اندروید حفظ و متن آن دقیق‌تر شد. برنامه به کاربر توضیح می‌دهد که در حالت عادی پیامک‌ها روی همان گوشی بررسی می‌شوند، برنامه دائماً در پس‌زمینه اجرا نمی‌شود و ثبت دستی و گزارش‌ها بدون مجوز پیامک نیز قابل استفاده‌اند.

در onboarding و راهنمای داخل برنامه نیز از عبارت مطلق «هیچ داده‌ای هرگز ارسال نمی‌شود» استفاده نشده و عبارت دقیق‌تر «در حالت عادی» به کار رفته است؛ چون اتصال‌های اختیاری مانند Drive یا Sheets فقط با اقدام کاربر می‌توانند فعال شوند.

### ۲.۷. شروع سریع‌تر و نمایش نتیجه واقعی اسکن

پس از انتخاب بازه اسکن اولیه، onboarding دیگر بلافاصله ناپدید نمی‌شود. ابتدا حالت «در حال بررسی پیامک‌ها» نشان داده می‌شود و پس از پایان، خلاصه واقعی اسکن نمایش داده می‌شود:

- تعداد پیامک‌های بررسی‌شده؛
- تعداد تراکنش‌های جدید؛
- تعداد پیامک‌های ناشناس موجود برای بررسی.

برای این کار، `OnboardingScanSummary` در `TransactionViewModel` اضافه شد و `ScanResult` موجود Repository مصرف می‌شود. تعداد پیامک‌های ناشناس از طریق `UnidentifiedSmsDao.countActive()` به‌صورت مستقیم از دیتابیس خوانده می‌شود.

دکمه ورود به برنامه تا پایان اسکن غیرفعال است. اگر اسکن خطا بدهد، جریان onboarding بسته می‌شود و پیام خطا از مسیر عادی برنامه نمایش داده خواهد شد.

### ۲.۸. ساده‌سازی نام تب‌ها و راهنما

برای مخاطب غیرمتخصص، نام‌های زیر استفاده شدند:

| نام قبلی | نام ساده‌تر |
|---|---|
| طرف‌حساب‌ها | طلب و بدهی |
| آمار | دخل و خرج |

تور اولیه و راهنمای داخل برنامه نیز با این نام‌ها هماهنگ شدند و توضیح داده شد که «طلب و بدهی» برای پول‌های دریافتی و پرداختی با اشخاص و «دخل و خرج» برای جمع درآمد و هزینه است.

## ۳. تغییرات فنی و فایل‌های اصلی

| فایل | تغییر |
|---|---|
| `ui/theme/Theme.kt` | RTL سراسری |
| `ui/theme/Type.kt` | اتصال Vazirmatn و افزایش خوانایی Typography |
| `ui/theme/Color.kt` | رنگ‌های عمیق‌تر برای کنتراست بهتر |
| `ui/screens/TransactionListScreen.kt` | کارت خلوت‌تر، مبلغ برجسته، حذف امن و Empty State دارای CTA |
| `ui/components/OnboardingFlow.kt` | مرحله نتیجه اسکن، متن حریم خصوصی و نام‌های ساده‌تر |
| `ui/TransactionViewModel.kt` | state نتیجه و وضعیت اجرای اسکن اولیه |
| `data/TransactionRepository.kt` | API شمارش پیامک‌های ناشناس فعال |
| `data/UnidentifiedSmsDao.kt` | query شمارش موارد ناشناس فعال |
| `permission/SmsPermissionGate.kt` | متن دقیق‌تر پیش از مجوز |
| `ui/screens/HelpScreen.kt` | هماهنگ‌سازی راهنمای داخلی |
| `MainActivity.kt` | نام‌های ساده‌تر تب‌ها |
| `app/build.gradle.kts` | نسخه `0.6.5` و کد `12` |
| `BuildInfo.kt` | همسان‌سازی نسخه، تاریخ و توضیح |
| `res/font/*` | فایل‌های فونت Vazirmatn |
| `licenses/Vazirmatn-copyright.txt` | ثبت مجوز فونت |

## ۴. مواردی که عمداً در این نسخه فعال نشدند

کارت گزارش ماهانه قابل اشتراک‌گذاری، ارسال پیامک ناشناس به‌صورت مستقیم، کانال تلگرام و دونیت در این نسخه فعال نشدند. این قابلیت‌ها به طراحی حریم خصوصی، آدرس رسمی، مسئول پشتیبانی، روش پرداخت یا سیاست کسب‌وکار نیاز دارند. فعال‌کردن آن‌ها بدون اطلاعات رسمی می‌تواند باعث نشت داده، دکمه بی‌کاربرد یا ادعای نادرست شود.

تست instrumentation واقعی برای Restore خراب نیز هنوز نیازمند emulator یا دستگاه Android است. تست واحد JVM اجرا شده، اما جای آزمایش دیتابیس واقعی و خطای وسط Restore را نمی‌گیرد.

## ۵. تست و بررسی ساخت

دستور اجراشده:

```bash
export JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64
export ANDROID_HOME=/home/ubuntu/android-sdk
./gradlew :app:testDebugUnitTest --no-daemon
```

نتیجه:

| شاخص | نتیجه |
|---|---:|
| تست‌های اجراشده | ۸۴ |
| شکست | ۰ |
| خطا | ۰ |
| وضعیت Gradle | `BUILD SUCCESSFUL` |

هشدارهای خروجی مربوط به deprecated بودن چند API آیکون و برخی متغیرهای استفاده‌نشده هستند و خطای کامپایل محسوب نمی‌شوند.

## ۶. نسخه نهایی

مقادیر نسخه در Gradle و BuildInfo همسان شدند:

```text
versionName = 0.6.5
versionCode = 12
BUILD_DATE = 2026-08-23
BUILD_DATE_SHAMSI = 1405-06-01
```

## ۷. محدودیت بررسی بصری

کامپایل و تست واحد موفق است. بااین‌حال، ارزیابی نهایی تجربه کاربری باید روی حداقل دو دستگاه یا emulator انجام شود: یکی با زبان سیستم فارسی و دیگری با زبان سیستم انگلیسی. در این بررسی باید جهت فلش‌ها، نوار پایین، FAB، متن ترکیبی SMS، خوانایی فونت و راحتی لمس دکمه‌ها دیده شود.

## نتیجه

نسخه v0.6.5 اصلاحات فوری UI/UX را با حفظ اصول KISS، YAGNI، Offline First و تأیید صریح کاربر اعمال می‌کند. مسیر اصلی برنامه اکنون برای کاربر فارسی‌زبان خواناتر، قابل‌فهم‌تر و کم‌خطرتر است؛ درحالی‌که قابلیت‌های آینده بدون اطلاعات یا سیاست لازم به‌صورت عجولانه وارد محصول نشده‌اند.
