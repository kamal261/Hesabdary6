# گزارش تغییرات نسخه v0.6.4

**نام پروژه:** SmsFinance / Hesabdary6  
**شاخه توسعه:** `manus-version`  
**نسخه:** `v0.6.4`  
**کد نسخه:** `11`  
**تاریخ ساخت:** `2026-08-23` / `1405-06-01`  
**توسعه‌دهنده و طراح:** سید کمال حقیقی  
**نوع تحویل:** سورس کامل در ZIP؛ بدون APK و بدون انتشار در GitHub

## خلاصه اجرایی

این نسخه برای تکمیل اصلاح BUG-02 آماده شد. در نسخه قبلی، cache مسیر دسته در صفحه اصلی و بخش اصلی انتخاب دسته اعمال شده بود، اما چند مسیر دیگر همچنان برای هر تراکنش `CategoryTree.pathOf(id, categories)` را مستقیم اجرا می‌کردند. در v0.6.4 همه این محل‌ها به نقشه مسیرهای آماده متصل شدند.

اکنون در مسیرهای UI و آمار، map دسته‌ها و مسیر کامل دسته برای همان فهرست دسته‌ها یک‌بار ساخته می‌شود و موارد بعدی با lookup ساده خوانده می‌شوند. این تغییر خروجی را عوض نمی‌کند؛ فقط محاسبات تکراری را کم می‌کند و در تاریخچه‌های بزرگ، فشار غیرضروری هنگام فیلتر، اسکرول و drill-down را کاهش می‌دهد.

## دامنه اصلاح BUG-02

| محل | تغییر انجام‌شده |
|---|---|
| `TransactionListScreen.kt` | کارت تراکنش و دیالوگ جزئیات از `categoryPaths` استفاده می‌کنند |
| `NotesScreen.kt` | جستجو، کارت یادداشت و دیالوگ جزئیات از map آماده استفاده می‌کنند |
| `TransactionDrilldownScreen.kt` | نمایش مسیر دسته برای ردیف‌های drill-down از lookup آماده استفاده می‌کند |
| `MainActivity.kt` | فیلتر `Overlay.CategoryTransactions` از map آماده استفاده می‌کند |
| `TransactionViewModel.kt` | `byCategory()` مسیرها را یک‌بار برای محاسبه آمار می‌سازد |
| `CategoryPicker.kt` | فهرست اصلی و دیالوگ ساخت شاخه، مسیرهای آماده را مصرف می‌کنند |

پس از اصلاح، در مسیرهای اصلی سورس دیگر استفاده مستقیم از `CategoryTree.pathOf` باقی نمانده است. تابع ساده `pathOf` همچنان در `CategoryTree` نگه داشته شده تا API خالص و تست‌پذیر قبلی حفظ شود؛ استفاده‌کنندگان سطح بالاتر اکنون از `pathsOf` استفاده می‌کنند.

## جزئیات فنی

تابع `CategoryTree.pathsOf(categories)` map دسته‌ها را یک‌بار می‌سازد و مسیر هر دسته را محاسبه می‌کند. در composableها این نتیجه با `remember(categories)` نگه‌داری می‌شود. در ViewModel که Compose در دسترس نیست، map در ابتدای `byCategory()` ساخته می‌شود و در طول همان محاسبه دوباره‌سازی نمی‌شود.

مسیرهای بدون دسته همچنان با متن `بدون دسته` نمایش داده می‌شوند. مسیرهای چندسطحی نیز بدون تغییر باقی مانده‌اند؛ برای مثال:

> هزینه / خرید خانه / سوپرمارکت / سیگار

این اصلاح فقط کارایی است و در منطق حسابداری، مبلغ، نوع تراکنش، روابط چک، طرف‌حساب یا انتقال شخصی تغییری ایجاد نمی‌کند.

## اصلاحات پایه حفظ‌شده از نسخه‌های قبلی

اصلاحات مهم نسخه‌های پیشین در این سورس باقی مانده‌اند:

- Restore جایگزین‌کننده اتمیک، به‌طوری که parse، پاک‌سازی و درج اطلاعات در مسیر امن انجام شوند؛
- گارد `TransactionCategoryRules` در UI و Repository برای جلوگیری از ثبت دسته ناسازگار با نوع تراکنش؛
- قفل‌بودن سمت هزینه/درآمد در مسیر عادی و باقی‌ماندن گزینه صریح «تغییر نوع» برای استثناها؛
- نمایش خودکار دسته هزینه برای برداشت و دسته درآمد برای واریز؛
- صفحه مستقل «همه یادداشت‌ها»؛
- نمایش مسیر کامل دسته در کارت‌های صفحه اصلی؛
- drill-down آمار بانک و دسته با نمایش متن کامل SMS و امکان کپی؛
- DedupEngine و `defaultCategoryName` که از نسخه‌های قبل وجود داشته‌اند و تغییر جدید این نسخه محسوب نمی‌شوند.

## تست نهایی

تست واحد با دستور زیر اجرا شد:

```bash
export JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64
export ANDROID_HOME=/home/ubuntu/android-sdk
./gradlew :app:testDebugUnitTest --no-daemon
```

نتیجه اجرای مستقل:

| شاخص | نتیجه |
|---|---:|
| تست‌های اجراشده | ۸۴ |
| شکست | ۰ |
| خطا | ۰ |
| نتیجه Gradle | `BUILD SUCCESSFUL` |

تست‌های موجود شامل تست‌های مسیر دسته، cache مسیرهای precomputed، قانون سازگاری نوع و دسته و سایر تست‌های قبلی پروژه هستند. تست instrumentation مستقیم برای فایل پشتیبان خراب و rollback Restore هنوز به emulator یا دستگاه Android نیاز دارد و در این اجرای JVM انجام نشده است.

## نسخه و مشخصات داخل برنامه

مقادیر زیر در هر دو محل همسان شدند:

- `app/build.gradle.kts`: `versionCode = 11` و `versionName = "0.6.4"`؛
- `app/src/main/java/com/kamal/smsfinance/BuildInfo.kt`: نسخه `0.6.4` و کد `11`؛
- تاریخ میلادی: `2026-08-23`؛
- تاریخ شمسی: `1405-06-01`؛
- نام توسعه‌دهنده و طراح: سید کمال حقیقی.

## بسته تحویل

ZIP نهایی شامل سورس کامل پروژه و گزارش‌های فارسی است. فایل‌های `.git`، `.gradle`، پوشه‌های `build`، `local.properties`، APK، AAB و `.hermes` در بسته قرار نگرفته‌اند. هیچ فایلی در GitHub منتشر نشده است.

## محدودیت باقی‌مانده

از نظر مسیرهای مستقیم `pathOf` در سورس اصلی، BUG-02 تکمیل شد. بااین‌حال، برای اطمینان بیشتر در آینده می‌توان یک تست instrumentation برای Restore خراب و یک تست UI برای بررسی نمایش مسیر در NotesScreen و drill-down اضافه کرد. این موارد مانع از تکمیل فیکس performance فعلی نیستند.

## نتیجه نهایی

نسخه v0.6.4 اصلاح ناقص BUG-02 را کامل کرد. همه پنج محل اصلی گزارش‌شده، به‌علاوه دیالوگ ساخت دسته که در گزارش اولیه جا افتاده بود، اصلاح شدند. هیچ فراخوانی مستقیم `CategoryTree.pathOf` در مسیرهای اصلی UI و آمار باقی نمانده است؛ تست‌های واحد نیز با ۸۴ موفقیت و بدون شکست اجرا شدند.
